################
### MONITORS ###
################

monitorv2 {
	output = eDP-1
	mode = preffered
	position = auto
	scale = 1.2
}

monitorv2 {
	output = HDMI-A-1
	mode = preffered
	position = auto
	scale = 1.2
	mirror = eDP-1
}

###################
### MY PROGRAMS ###
###################

$terminal = foot
$neo = foot -eH fastfetch --config archey.jsonc
$fileManager = lf
$browser = librewolf 
$menu = rofi -show drun
$rootbrowser = rofi -show recursivebrowser
$filebrowser = rofi -show recursivebrowser

#################
### AUTOSTART ###
#################

exec-once = $terminal
exec-once = waybar
exec-once = hyprpaper & hypridle
exec-once = systemctl --user start hyprpolkitagent
exec-once = nm-applet
exec-once = rfkill unblock bluetooth
exec-once = clipcatd

#############################

env = XCURSOR_SIZE, 20
env = HYPRCURSOR_SIZE, 20
env = GTK_SCALE, 1

xwayland {
	force_zero_scaling = true
}

###################
### PERMISSIONS ###
###################

ecosystem {
    
}

debug {
	disable_logs = false

	damage_tracking = 0
}

#####################
### LOOK AND FEEL ###
#####################

general {
    gaps_in = 1
    gaps_out = 6

    border_size = 1

    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    resize_on_border = false

    allow_tearing = false

    layout = dwindle
}

decoration {
	rounding = 0
	rounding_power = 0

    active_opacity = 0.9
    inactive_opacity = 0.8

    shadow {
        enabled = false
    }

    blur {
        enabled = true
        size = 2
        passes = 2

        vibrancy = 0.1696
    }
}

animations {
    enabled = yes, please :)

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, gnomed
    animation = windowsOut, 1, 1.49, linear, gnomed
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 2, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 0.8, almostLinear
    animation = fadeLayersOut, 1, 0.6, easeInOutCubic
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

dwindle {
    preserve_split = true # You probably want this
}

master {
    new_status = master
}

misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

input {
    kb_layout = de
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0.3 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

$mainMod = SUPER
$shiftMod = SUPER_SHIFT
$ctrlMod = SUPER_CTRL
$ctrlShiftMod = SUPER_CTRL_SHIFT

bind = $mainMod, Q, exec, $terminal
bind = $mainMod, N, exec, $neo
bind = $mainMod, C, killactive,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bindr = $mainMod, Super_L , exec, pkill rofi || $menu -replace
bind = $shiftMod, V, exec, clipcat-menu
bind = $mainMod, F, exec, pkill rofi || $filebrowser
binds = $mainMod, R&F, exec, pkill rofi || $rootbrowser
bind = $mainMod, W, exec, pkill waybar || waybar
bind = $mainMod, B, exec, $browser
bind = $mainMod, D, exec, vesktop --ozone-platform-hint=auto
bind = $mainMod, L, exec, hyprlock
bind = $mainMod, S, exec, hyprshot -m active -m output -szo ~/Documents/screenshots
bind = $shiftMod, S, exec, hyprshot -m region -szo ~/Documents/screenshots
bind = $ctrlMod, S, exec, hyprshot -m window -szo ~/Documents/screenshots
bind = $ctrlShiftMod, S, setprop, active opaque toggle
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, T, exec, swaync-client -t
bind = $shiftMod, P, exec, hyprpicker -r -a -f hex
bind = $ctrlMod, P, exec, hyprpicker -r -a -f rgb

bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod, A, togglespecialworkspace, magic
bind = $mainMod SHIFT, A, movetoworkspace, special:magic

bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 2.5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 2.5%-

bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioStop, exec, playerctl stop
bindl = , XF86AudioPlay, exec, playerctl play-pause 
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

windowrule {
	name = meh
	match:class = .*

	suppress_event = maximize
} 
